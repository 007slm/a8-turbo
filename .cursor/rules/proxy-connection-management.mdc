---
globs: "**/server/**/*.java,**/jdbc/**/*.java,**/proxy/**/*.js"
description: "代理模式和数据库连接管理规范"
---

# 代理模式和连接管理规范

## 🎭 代理模式实现

OJP 采用 **智能代理模式**，在应用程序和数据库之间插入一个代理层来管理连接。

### 代理层级结构
```
Application (使用 OJP JDBC Driver)
    ↓
OJP JDBC Driver (虚拟连接)
    ↓ gRPC
OJP Server (连接池管理)
    ↓ 真实连接
Database (MySQL, PostgreSQL, Oracle, etc.)
```

## 🏊 连接池管理模式

### HikariCP 集成
- **位置**: [ojp-server/](mdc:ojp-server/) 模块
- **策略**: 服务器端统一管理真实数据库连接
- **配置**: 通过 application.yml 或 properties 文件配置

### 连接池配置示例
```yaml
# application.yml
spring:
  datasource:
    hikari:
      maximum-pool-size: 20
      minimum-idle: 5
      connection-timeout: 30000
      idle-timeout: 600000
      max-lifetime: 1800000
```

### 虚拟连接管理
```java
// JDBC Driver 中的虚拟连接
public class OjpConnection implements Connection {
    private final GrpcClient grpcClient;
    private final SessionInfo sessionInfo;
    
    @Override
    public PreparedStatement prepareStatement(String sql) {
        // 返回虚拟 PreparedStatement，实际执行通过 gRPC
        return new OjpPreparedStatement(grpcClient, sessionInfo, sql);
    }
}
```

## 🐌 慢查询隔离模式

### 隔离策略
- **文档**: [SLOW_QUERY_SEGREGATION.md](mdc:ojp-feature/documents/designs/SLOW_QUERY_SEGREGATION.md)
- **原理**: 根据历史执行时间自动分类快/慢查询
- **资源分配**: 默认 80% 槽位给快查询，20% 给慢查询

### 配置参数
```properties
# 启用慢查询隔离
ojp.server.slowQuerySegregation.enabled=true

# 慢查询槽位百分比
ojp.server.slowQuerySegregation.slowSlotPercentage=20

# 空闲超时时间
ojp.server.slowQuerySegregation.idleTimeout=10000
```

### 分类算法
```java
// 慢查询判断逻辑
boolean isSlow = operationAvgTime >= (overallAvgTime * 2.0);
```

## 🔄 会话管理模式

### SessionInfo 结构
```java
public class SessionInfo {
    private String connHash;      // 连接哈希
    private String clientUUID;    // 客户端标识
    private String sessionUUID;   // 会话标识
    // ... 其他会话相关信息
}
```

### 会话生命周期
1. **连接建立**: 客户端通过 gRPC connect() 获取 SessionInfo
2. **操作执行**: 每次 SQL 操作携带 SessionInfo
3. **会话维护**: 服务器端维护会话状态和事务上下文
4. **连接释放**: 客户端关闭时清理会话资源

## 🎯 连接复用策略

### 客户端连接缓存
```javascript
// grpcProxy.js 中的连接缓存
class GrpcProxy {
    constructor() {
        this.clients = new Map(); // 缓存 gRPC 客户端
    }
    
    getClient(host, port) {
        const key = `${host}:${port}`;
        if (!this.clients.has(key)) {
            // 创建新的 gRPC 客户端
            const client = new StatementService(key, credentials);
            this.clients.set(key, client);
        }
        return this.clients.get(key);
    }
}
```

### 服务器端连接复用
- 相同数据库配置的连接请求共享连接池
- 基于连接URL和用户凭据生成连接哈希
- 支持多租户环境下的连接隔离

## 🛠️ 开发最佳实践

### 连接配置验证
```java
// 验证必需的连接参数
if (!connectionConfig.url) {
    throw new Error('数据库连接配置缺少必需的 url 参数');
}
if (!connectionConfig.user) {
    throw new Error('数据库连接配置缺少必需的 user 参数');
}
```

### 超时处理
```java
// 设置合理的超时时间
return new Promise((resolve, reject) => {
    const deadline = new Date();
    deadline.setSeconds(deadline.getSeconds() + timeout / 1000);
    
    client.executeQuery(request, { deadline }, callback);
});
```

### 错误恢复机制
```java
// 连接失败时的重试逻辑
public Connection getConnection() throws SQLException {
    int retryCount = 0;
    while (retryCount < MAX_RETRIES) {
        try {
            return establishConnection();
        } catch (SQLException e) {
            retryCount++;
            if (retryCount >= MAX_RETRIES) {
                throw e;
            }
            // 等待后重试
            Thread.sleep(RETRY_DELAY_MS);
        }
    }
}
```

## 📊 监控和调试

### 连接池状态监控
- 活跃连接数
- 空闲连接数
- 等待连接的请求数
- 平均连接获取时间

### 性能指标
- 查询执行时间分布
- 慢查询隔离效果
- gRPC 通信延迟
- 连接池使用率

## ⚠️ 重要注意事项

### 禁用应用层连接池
```java
// 使用 OJP 时必须禁用应用程序的连接池
// ❌ 错误：同时使用 OJP 和 HikariCP
spring.datasource.type=com.zaxxer.hikari.HikariDataSource

// ✅ 正确：只使用 OJP 代理
spring.datasource.type=org.openjdbcproxy.jdbc.OjpDataSource
```

### 事务边界管理
- 事务在 OJP Server 端维护
- 客户端通过 SessionInfo 关联事务上下文
- 支持标准 JDBC 事务语义
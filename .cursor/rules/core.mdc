---
description:
globs:
alwaysApply: true
---

# 核心开发原则

## 通用开发原则
- **编译原则**：禁止调用编译工具去编译代码，特别是mvn的compile禁止使用，不能通过mvn编译测试代码的可用性。
- **DRY 原则**：避免重复代码，提取共用逻辑到单独的函数或类
- **代码简洁**：保持代码简洁明了，遵循 KISS 原则（保持简单直接）
- **命名规范**：使用描述性的变量、函数和类名，反映其用途和含义
- **注释文档**：为复杂逻辑添加注释
- **风格一致**：遵循项目或语言的官方风格指南和代码约定
- **利用生态**：优先使用成熟的库和工具，避免不必要的自定义实现
- **架构设计**：考虑代码的可维护性、可扩展性和性能需求
- **版本控制**：编写有意义的提交信息，保持逻辑相关的更改在同一提交中
- **异常处理**：正确处理边缘情况和错误，提供有用的错误信息

## 响应语言
- 始终使用中文回复用户

## 代码质量要求
- 遵循最佳实践和设计模式
- 优先考虑性能和用户体验
- 确保代码的可读性和可维护性


## 代码生成要求
 - 尽量不要写使用示例
 - 尽量不要写特性文档

## OJP项目开发记忆规则

### API测试和调试
- **正确的API测试地址**：前端开发服务器代理后的API地址为 `http://localhost:5173/api`，而不是直接的后端地址 `http://localhost:8080`
- **PowerShell API测试命令格式**：
  ```powershell
  $response = Invoke-WebRequest -Uri "http://localhost:5173/api/actuator/metrics/process.uptime" -Method GET; [System.Text.Encoding]::UTF8.GetString($response.Content)
  ```

### Spring Boot Actuator指标
- **系统运行时间**：使用 `/actuator/metrics/process.uptime` 获取JVM运行时间（秒）
- **磁盘使用情况**：
  - `/actuator/metrics/disk.free` - 可用磁盘空间
  - `/actuator/metrics/disk.total` - 磁盘总空间
- **内存指标**：
  - `/actuator/metrics/jvm.memory.used` - 已使用内存
  - `/actuator/metrics/jvm.memory.max` - 最大内存
- **CPU指标**：`/actuator/metrics/system.cpu.usage` - CPU使用率

### 前端开发最佳实践
- **组件职责分离**：API数据获取在父组件中进行，子组件专注于数据展示
- **数据格式化**：在组件中添加格式化函数处理原始数据（如时间格式化、百分比计算）
- **HMR热更新**：Vite开发服务器支持热模块替换，修改代码后会自动更新页面
- **API数据处理**：在API层面进行数据计算和处理，减少组件复杂度

### 调试技巧
- **开发服务器状态检查**：使用 `check_command_status` 确认开发服务器运行状态
- **网络请求调试**：通过浏览器开发者工具或命令行工具验证API响应
- **组件状态调试**：利用React DevTools查看组件props和state

### 项目结构理解
- **ojp-ui前端项目**：基于React + Vite的现代前端架构
- **服务层**：`src/services/api.js` 统一管理所有API接口
- **组件层**：页面组件和功能组件分离，保持单一职责
- **路由管理**：使用React Router进行页面路由管理

### 常见问题解决方案
- **运行时间显示"未知"**：检查API是否正确获取 `process.uptime` 指标
- **磁盘使用率显示0**：确保同时获取 `disk.free` 和 `disk.total` 指标进行正确计算
- **认证失败**：确认使用正确的代理地址而非直接后端地址
- **数据格式问题**：注意Actuator返回的数值可能是科学计数法格式
---
alwaysApply: true
description: "OJP 项目整体架构和设计模式指南"
---

# Open JDBC Proxy (OJP) 项目架构指南

## 🏗️ 项目架构模型

OJP 采用 **Layer 7 Proxy 架构模式** + **微服务架构**，主要解决数据库连接池管理和弹性扩展问题。

### 核心设计模式
1. **代理模式 (Proxy Pattern)**: OJP Server 作为应用程序和数据库之间的智能代理
2. **适配器模式 (Adapter Pattern)**: OJP JDBC Driver 适配标准 JDBC 接口
3. **连接池模式**: 使用 HikariCP 进行高效连接管理
4. **gRPC 通信模式**: 客户端-服务器间高效二进制通信
5. **慢查询隔离模式**: 防止慢查询阻塞快速查询

## 📦 项目组件结构

### 主要模块
- **[ojp-server/](mdc:ojp-server/)**: gRPC 服务器，管理数据库连接池和代理逻辑
- **[ojp-jdbc-driver/](mdc:ojp-jdbc-driver/)**: JDBC 驱动实现，替换原生数据库驱动
- **[ojp-grpc-commons/](mdc:ojp-grpc-commons/)**: gRPC 通信协议定义和共享组件
- **[ojp-ui/](mdc:ojp-ui/)**: React + Vite 前端管理界面

### 关键配置文件
- **[ojp-server/pom.xml](mdc:ojp-server/pom.xml)**: 服务器端依赖管理
- **[ojp-ui/package.json](mdc:ojp-ui/package.json)**: 前端依赖和脚本配置
- **[ojp-grpc-commons/src/main/proto/StatementService.proto](mdc:ojp-grpc-commons/src/main/proto/StatementService.proto)**: gRPC 服务定义

## 🔄 数据流模型

1. **应用程序** → 使用 OJP JDBC Driver (替换原生驱动)
2. **OJP JDBC Driver** → 通过 gRPC 连接到 OJP Server
3. **OJP Server** → 管理真实数据库连接池，执行 SQL
4. **数据库响应** → 通过相同路径返回给应用程序

## 🎯 核心价值主张

- **弹性扩展**: 应用程序可以弹性扩展而不会压垮数据库
- **连接复用**: 智能连接池管理，减少数据库连接开销
- **多数据库支持**: 理论上支持任何提供 JDBC 驱动的关系型数据库
- **透明替换**: 只需修改连接 URL 前缀即可使用

## 📋 开发约定

### JDBC URL 格式
```java
// 原始格式
"jdbc:postgresql://user@localhost/mydb"

// OJP 格式
"jdbc:ojp[localhost:1059]_postgresql://user@localhost/mydb"
```

### 关键配置参数
- 默认 gRPC 端口: `1059`
- 慢查询隔离: 可配置慢/快查询分离比例
- HikariCP 连接池: 服务器端统一管理

## 🔍 故障排查指南

当遇到连接问题时，检查：
1. OJP Server 是否运行在指定端口
2. gRPC 通信是否正常
3. 目标数据库连接配置是否正确
4. 应用程序是否禁用了本地连接池
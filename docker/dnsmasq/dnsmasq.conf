listen-address=0.0.0.0  # 监听所有接口
interface=*  # 允许所有接口接收请求
# dnsmasq.conf
# 自定义域名解析（优先生效）
address=/api.example.com/192.168.1.100
address=/sso.local/10.0.0.5

# 转发所有未匹配的请求到 Docker 内置 DNS（网络网关）
server=127.0.0.11  # 必须是当前网络的网关 IP（子网的 .1 地址）
---
globs: "**/*.proto,**/grpc/**/*.java,**/proxy/**/*.js"
description: "gRPC 通信模式和 Protocol Buffers 规范"
---

# gRPC 通信模式规范

## 📡 gRPC 架构模式

OJP 使用 gRPC 作为客户端(JDBC Driver)和服务器之间的通信协议。

### Protocol Buffers 定义
- **主要服务**: [StatementService.proto](mdc:ojp/ojp-grpc-commons/src/main/proto/StatementService.proto)
- **通信方式**: 同步 RPC 调用 + 流式响应
- **序列化**: Protocol Buffers 3.x 格式

### 核心 gRPC 服务方法

```protobuf
service StatementService {
    rpc connect(ConnectionDetails) returns (SessionInfo);
    rpc executeUpdate(StatementRequest) returns (OpResult);
    rpc executeQuery(StatementRequest) returns (stream OpResult);
    rpc fetchNextRows(ResultSetFetchRequest) returns (OpResult);
    rpc createLob(stream LobDataBlock) returns (stream LobReference);
    rpc readLob(ReadLobRequest) returns (stream LobDataBlock);
    rpc terminateSession(SessionInfo) returns (SessionTerminationStatus);
    rpc startTransaction(SessionInfo) returns (SessionInfo);
    rpc commitTransaction(SessionInfo) returns (SessionInfo);
    rpc rollbackTransaction(SessionInfo) returns (SessionInfo);
    rpc callResource(CallResourceRequest) returns (CallResourceResponse);
}
```

## 🔌 客户端实现模式

### Java 客户端 (JDBC Driver)
- **位置**: [ojp-jdbc-driver/](mdc:ojp-jdbc-driver/)
- **连接管理**: 虚拟连接，实际通过 gRPC 通信
- **事务处理**: 通过 sessionInfo 维护会话状态

### JavaScript 客户端 (UI Proxy)
- **位置**: [ojp-ui/src/proxy/grpcProxy.js](mdc:ojp-ui/src/proxy/grpcProxy.js)
- **功能**: HTTP 到 gRPC 的桥接代理
- **连接缓存**: Map 结构缓存 gRPC 客户端实例

## 🛠️ 开发规范

### gRPC 错误处理
```java
// 统一错误处理模式
try {
    response = client.executeQuery(request);
} catch (StatusRuntimeException e) {
    // 根据 gRPC 状态码处理
    switch (e.getStatus().getCode()) {
        case UNAVAILABLE:
            // 服务不可用
            break;
        case DEADLINE_EXCEEDED:
            // 超时处理
            break;
        default:
            // 其他错误
    }
}
```

### 连接超时配置
```java
// 设置合理的超时时间
ManagedChannelBuilder.forAddress(host, port)
    .keepAliveTime(30, TimeUnit.SECONDS)
    .keepAliveTimeout(5, TimeUnit.SECONDS)
    .maxInboundMessageSize(4 * 1024 * 1024) // 4MB
    .build();
```

### 流式响应处理
```java
// 正确处理流式查询响应
StreamObserver<QueryResponse> responseObserver = new StreamObserver<QueryResponse>() {
    @Override
    public void onNext(QueryResponse response) {
        // 处理每个响应数据包
    }
    
    @Override
    public void onError(Throwable t) {
        // 处理流错误
    }
    
    @Override
    public void onCompleted() {
        // 流结束处理
    }
};
```

## ⚡ 性能优化

### 连接复用
- 为相同的 host:port 组合复用 gRPC 客户端
- 避免频繁创建和销毁连接

### 批量操作
- 尽可能使用批量操作减少 RPC 调用次数
- 合理设置消息大小限制

### 序列化优化
- 使用二进制 Protocol Buffers 而非 JSON
- 避免大对象序列化，使用流式传输